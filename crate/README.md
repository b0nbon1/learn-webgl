# Second webgl That creates a 3D rotating cube
-----

*fill this later*

![](./rotating-crate.png)

### To run the app:

- clone the repo and run `cd learn-webgl/crate`
- open the `index.html` with your browser